; `!` answers exactly what its body answered, and marks it to outlive the run.
; expect: calls 1
; expect: clean
; expect: answers
; expect: contains dream
(! "Answer with exactly the word: remembered")
