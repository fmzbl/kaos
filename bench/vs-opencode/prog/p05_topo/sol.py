def topo_sort(nodes, edges):
    """Return nodes in dependency order. edges are (a, b) meaning a must come before b.
    Return None when the graph has a cycle. Break ties by picking the smallest
    available node so the result is deterministic."""
    raise NotImplementedError
